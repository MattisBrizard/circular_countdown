name: example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

publish_to: none

dependencies:
  circular_countdown:
    path: ../
  flutter:
    sdk: flutter
  gap: ^2.0.0
  timer_controller: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
